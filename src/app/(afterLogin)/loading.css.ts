import { style } from "@vanilla-extract/css";

const wrapper = style({
	width: "100%",
	height: "100%",
	display: "flex",
	justifyContent: "center",
	alignItems: "center",
});

const ve = { wrapper };
export default ve;
